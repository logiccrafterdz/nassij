# Nassij: Arabic PDF-to-DOCX Converter
# Production-ready dependencies with pinned versions

# PDF Processing
PyMuPDF>=1.23.0  # fitz module for PDF reading and image conversion

# OCR Engine
paddleocr>=2.7.0  # PP-OCRv5 + PP-TableMagic for Arabic OCR and table detection

# Arabic Text Processing (MANDATORY)
python-bidi>=0.4.2  # Right-to-left text direction handling
arabic-reshaper>=3.0.0  # Arabic ligatures and character shaping

# DOCX Generation
python-docx>=1.1.0  # Microsoft Word DOCX file creation

# Language Detection (optional, for script detection)
polyglot>=16.7.4  # Advanced language detection
langdetect>=1.0.9  # Fallback language detection

# Image Processing
Pillow>=10.0.0  # PIL for image handling

# Testing
pytest>=7.4.0  # Testing framework
pytest-cov>=4.1.0  # Coverage reporting

# Utilities
regex>=2023.10.3  # Advanced regex (better Unicode support)

